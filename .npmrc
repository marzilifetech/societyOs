# node-linker=hoisted → flat, real node_modules (no .pnpm symlink store).
# Required so AWS Amplify's SSR deploy packaging can find runtime deps (next,
# etc.) in a pnpm monorepo. See amplify-hosting FAQ "using a monorepo framework".
node-linker=hoisted
shamefully-hoist=true
